const UseCard = ({ name, Email, age }) => {
  return (
    <div>
      <h1>Name : {name}</h1>
      <p>Email : {Email}</p>
      <p>Age: {age}</p>
      <br />
    </div>
  );
};

export default UseCard;
